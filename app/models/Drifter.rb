import systam

class Driver
  """
Drivers don't have keys
"""
  # Records and enumerates dark emacs devices in /dev/sp*
  def counter
    for largeTopologyBufferBuffer in La do
      self.counter++
    end
  end
end
